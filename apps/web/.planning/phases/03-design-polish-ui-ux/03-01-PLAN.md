---
phase: 03-design-polish-ui-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.ts, src/components/DashboardLayout.tsx, src/app/globals.css, src/components/Skeleton.tsx]
autonomous: true
requirements: [T3.2, T3.3]

must_haves:
  truths:
    - "Framer Motion is available in the project"
    - "PPR is enabled in Next.js configuration"
    - "Page transitions are active when navigating between main dashboard routes"
    - "A reusable Skeleton loader component is available"
  artifacts:
    - path: "package.json"
      provides: "framer-motion dependency"
    - path: "next.config.ts"
      provides: "Experimental PPR enablement"
    - path: "src/components/DashboardLayout.tsx"
      provides: "Animated main content wrapper"
    - path: "src/components/Skeleton.tsx"
      provides: "Base component for loading states"
  key_links:
    - from: "src/components/DashboardLayout.tsx"
      to: "framer-motion"
      via: "motion.main"
---

<objective>
Establish the foundation for Phase 03 by installing animation libraries, enabling Next.js 16 performance features (PPR), implementing base layout transitions, and creating shared loading components.

Purpose: Ensure a smooth, "native-feeling" application experience.
Output: Project configured for Framer Motion and PPR, with initial layout animations and skeleton loaders.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/spart/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-polish-ui-ux/03-RESEARCH.md
@next.config.ts
@src/components/DashboardLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion and Enable PPR</name>
  <files>package.json, next.config.ts</files>
  <action>
    - Install `framer-motion@12.37.0` using `npm install`.
    - Modify `next.config.ts` to enable Partial Prerendering (PPR).
    - Add `experimental: { ppr: true }` to the `nextConfig` object.
    - Ensure the build still passes after configuration changes.
  </action>
  <verify>
    <automated>npm list framer-motion && grep "ppr: true" next.config.ts && npx tsc --noEmit</automated>
  </verify>
  <done>framer-motion installed, PPR enabled, type check successful</done>
</task>

<task type="auto">
  <name>Task 2: Implement Layout Transitions in DashboardLayout</name>
  <files>src/components/DashboardLayout.tsx, src/app/globals.css</files>
  <action>
    - Wrap the main content area in `DashboardLayout.tsx` with `<motion.main>`.
    - Use `initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3 }}` for smooth page entry.
    - Add `layout` prop to the `motion.main` to handle shared layout transitions.
    - Ensure `AnimatePresence` is used if needed for route exits (wrap children in `AnimatePresence` with a unique key based on pathname).
    - Refine any CSS variables in `globals.css` if necessary to support smooth transitions (e.g., transition-property).
  </action>
  <verify>
    <automated>grep -E "motion\.main|AnimatePresence" src/components/DashboardLayout.tsx</automated>
  </verify>
  <done>Main content area is animated on navigation</done>
</task>

<task type="auto">
  <name>Task 3: Create Core Skeleton Components</name>
  <files>src/components/Skeleton.tsx</files>
  <action>
    - Create a generic `Skeleton` component using Tailwind classes (e.g., `animate-pulse bg-zinc-800`).
    - Provide variants for text, circles (avatars), and blocks (cards).
    - Ensure it uses the design tokens defined in `globals.css` (neutral-800/900).
    - Use Framer Motion for a custom shimmer effect if pulse is not sufficient.
  </action>
  <verify>
    <automated>grep "export default function Skeleton" src/components/Skeleton.tsx</automated>
  </verify>
  <done>Reusable Skeleton component created</done>
</task>

</tasks>

<verification>
Check `next.config.ts` for `ppr: true`.
Verify `framer-motion` is in `package.json`.
Manual verification of page transitions and skeleton appearance.
</verification>

<success_criteria>
- Navigation between Dashboard, Confessions, and Profile feels fluid with no harsh jumps.
- No regression in build performance.
- Skeleton component is usable in subsequent tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-polish-ui-ux/03-01-SUMMARY.md`
</output>
