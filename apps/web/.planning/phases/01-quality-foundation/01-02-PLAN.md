---
phase: 01-quality-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [package.json, src/lib/validation/schemas.ts, src/types/profile.ts, src/types/groups.ts, src/components/MessageItem.tsx]
autonomous: true
requirements: [T1.1, T1.3, QUAL-01]

must_haves:
  truths:
    - "MessageItem.tsx uses strict TypeScript (no 'any')"
    - "Data models are validated using Zod at the boundary"
    - "Manual normalization logic is replaced with schema-driven validation"
  artifacts:
    - path: "src/lib/validation/schemas.ts"
      provides: "Zod schemas for all core models"
    - path: "src/types/profile.ts"
      provides: "Refined UserProfile type derived from Zod"
    - path: "src/types/groups.ts"
      provides: "Unified Message type"
  key_links:
    - from: "src/types/profile.ts"
      to: "src/lib/validation/schemas.ts"
      via: "z.infer"
---

<objective>
Eliminate the 'any' type from the codebase and standardize data validation using Zod.

Purpose: Ensure runtime data integrity and provide developer confidence through strict compile-time types.
Output: Comprehensive Zod schemas and strictly typed data models.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/spart/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-RESEARCH.md
@.planning/phases/01-quality-foundation/01-01-SUMMARY.md
@src/types/profile.ts
@src/types/groups.ts
@src/components/MessageItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Core Zod Schemas</name>
  <files>package.json, src/lib/validation/schemas.ts</files>
  <action>
    - Install `zod`.
    - Create `src/lib/validation/schemas.ts`.
    - Define Zod schemas for: `UserProfile`, `Group`, `GroupMessage`, `Message` (unify `GroupMessage` and `Message`).
    - Include sub-schemas for `GalleryItem`, `StoryItem`, `HighlightItem`, `NotificationPreferences`, etc.
    - Ensure `timestamp` is handled correctly as `Firebase Timestamp` (use `z.any()` only for the underlying SDK type if needed, but typed at the application layer).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zod schemas are defined for all core entities.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Manual Normalization with Zod</name>
  <files>src/types/profile.ts, src/types/groups.ts</files>
  <action>
    - Update `src/types/profile.ts` and `src/types/groups.ts` to infer types from Zod schemas using `z.infer`.
    - Refactor `normalizeUserProfile` in `profile.ts` to use `userProfileSchema.parse()` or `safeParse()`.
    - Remove redundant manual normalization helper functions (e.g., `normalizeGallery`).
    - Unify `Message` and `GroupMessage` into a single, strictly typed `Message` interface.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Data models are schema-driven and manual normalization is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Eliminate 'any' in MessageItem.tsx</name>
  <files>src/components/MessageItem.tsx</files>
  <action>
    - Update `MessageItem.tsx` to use the unified `Message` type from `src/types/`.
    - Replace `timestamp?: any` with `Timestamp | Date | null` as appropriate.
    - Ensure all props and internal variables are strictly typed.
    - Add a unit test `src/components/MessageItem.test.tsx` using Vitest to verify rendering with strict types.
  </action>
  <verify>
    <automated>npm test src/components/MessageItem.test.tsx</automated>
  </verify>
  <done>MessageItem.tsx is strictly typed and verified with a test.</done>
</task>

</tasks>

<verification>
Check for remaining `any` usage in `src/types/` and `src/components/MessageItem.tsx`.
Ensure `npm run lint` and `npx tsc` pass.
</verification>

<success_criteria>
- [ ] No `: any` found in `src/types/` or `MessageItem.tsx`.
- [ ] Zod schemas cover all core data models.
- [ ] Manual normalization logic is fully replaced by Zod.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-02-SUMMARY.md`
</output>
