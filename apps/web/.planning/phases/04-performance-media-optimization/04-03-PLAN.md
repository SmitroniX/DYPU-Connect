---
phase: 04-performance-media-optimization
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified: [next.config.ts, public/manifest.json]
autonomous: true
requirements: [T4.3, T4.4]
user_setup: []

must_haves:
  truths:
    - "Service worker caches messaging and profile data for offline access"
    - "PWA manifest correctly reflects application theme and display options"
    - "Lighthouse scores for performance and best practices show significant improvement"
  artifacts:
    - path: "next.config.ts"
      provides: "Configured runtimeCaching for PWA"
    - path: "public/manifest.json"
      provides: "Updated PWA metadata"
---

<objective>
Enhance the PWA/offline capabilities of the application and perform a final performance audit to verify the improvements made during Phase 04.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-performance-media-optimization/04-RESEARCH.md
@next.config.ts
@public/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA and Service Worker Enhancements</name>
  <files>next.config.ts, public/manifest.json</files>
  <action>
    1. Update `next.config.ts` to include specialized `runtimeCaching` for Firestore/FDC API calls and Firebase Storage assets.
    2. Ensure `skipWaiting` and `register` are correctly configured.
    3. Update `public/manifest.json` with appropriate icons, colors, and the correct `scope` and `start_url`.
  </action>
  <verify>
    <automated>grep "runtimeCaching" next.config.ts</automated>
  </verify>
  <done>PWA configuration improved for better offline reliability.</done>
</task>

<task type="auto">
  <name>Task 2: Final Performance Audit and Optimization</name>
  <files>package.json</files>
  <action>
    1. Run a production build (`npm run build`).
    2. Perform a Lighthouse audit for a representative set of pages (Dashboard, Public Chat, Profile).
    3. Analyze results for any remaining performance bottlenecks.
    4. Address any low-hanging fruit optimizations (unused CSS, inefficient script loading, etc.).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 04 final performance audit completed.</done>
</task>

</tasks>

<verification>
Verify build output for service worker generation.
Manual verification of manifest loading in browser.
</verification>

<success_criteria>
- runtimeCaching is defined in next.config.ts.
- PWA manifest loads correctly.
- Performance scores meet or exceed targets defined in 04-VALIDATION.md.
</success_criteria>
