d22c5fc refine: security, performance, error handling, and Google Drive improvements - Fix Permissions-Policy: allow camera/microphone (self) for VideoCall - Add lh3.googleusercontent.com to Next.js image remote patterns - Add media-src blob: and wss: to CSP for WebRTC streams - Fix sanitiseInput: use global regex flags to strip ALL XSS matches - Add svg, math, vbscript patterns to XSS filter - Add error.tsx error boundaries (root + admin) - Add loading.tsx skeleton for better perceived performance - Deduplicate Google Drive upload logic (shared _multipartUpload helper) - Add retry logic (fetchWithRetry) to all Google Drive API calls - Fix script load promise not resetting on failure - Add limit(500) to admin users Firestore query - Tighten Firestore rules: restrict WebRTC call writes to participants - Deploy updated Firestore rules
63233a9 fix: add missing driveConnected variable in profile page (fixes Netlify build)
8915b7a fix: null safety on userProfile.googleDrive.folderId (deploy fix)
2fd9bd8 fix: unblock Pick Image buttons ΓÇö remove silent disable gate The Pick Image / Upload Photo buttons were silently disabled because canUploadToDrive required both:   1. NEXT_PUBLIC_GOOGLE_CLIENT_ID env var (missing on Netlify)   2. googleDrive field on user profile (missing for old users) Fixes: - Buttons are now always clickable (canUploadToDrive = true) - Upload function shows clear error if GOOGLE_CLIENT_ID is missing - Auto-fixes profiles missing googleDrive field at upload time - Removed misleading 'Connect Google Drive in Settings' warnings
3e9a907 fix: photo/story upload ΓÇö cache Drive token + reliable image URLs Root causes: 1. Drive access token was never saved to the store after the GSI    popup, so every upload triggered a new consent popup (which    browsers often block as a second popup) 2. Google Drive image URL format (uc?export=view) is unreliable ΓÇö    Google frequently blocks it with a 'can''t process' interstitial Fixes: - uploadImageFileToDrive now calls setDriveAccessToken() after   getting a fresh token, so subsequent uploads reuse it - On upload error, token is cleared so next attempt gets a fresh one - Changed direct image URL from drive.google.com/uc?export=view   to lh3.googleusercontent.com/d/{fileId}=s1600 which is the   reliable Google Photos/Drive thumbnail endpoint
