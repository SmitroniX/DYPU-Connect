---
phase: 04-performance-media-optimization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/app/layout.tsx, src/app/messages/[chatId]/page.tsx, src/components/MessageItem.tsx, src/lib/data.ts, dataconnect/main/operations.gql]
autonomous: true
requirements: [T4.2]
user_setup: []

must_haves:
  truths:
    - "Server-side data fetching for profiles and groups uses the 'use cache' directive"
    - "Main messaging and dashboard routes utilize PPR effectively"
    - "BlurHash previews are displayed while images are loading"
  artifacts:
    - path: "src/lib/data.ts"
      provides: "Cached data fetching functions"
    - path: "src/components/MessageItem.tsx"
      provides: "BlurHash preview support for image messages"
    - path: "dataconnect/main/operations.gql"
      provides: "Queries updated to fetch blurHash"
---

<objective>
Optimize application performance by utilizing Next.js 16's 'use cache' directive, ensuring efficient Partial Prerendering (PPR) boundaries, and implementing BlurHash previews in the UI.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-performance-media-optimization/04-RESEARCH.md
@src/app/layout.tsx
@src/components/MessageItem.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 'use cache' for common data fetching</name>
  <files>src/lib/data.ts</files>
  <action>
    1. Create `src/lib/data.ts` (or update existing).
    2. Implement functions for fetching student profiles, group metadata, and public chat info.
    3. Annotate these functions with the `"use cache"` directive (Next.js 16).
    4. Configure revalidation tags/times as researched.
  </action>
  <verify>
    <automated>grep "\"use cache\"" src/lib/data.ts</automated>
  </verify>
  <done>Semi-static data fetching is now cached at the server level.</done>
</task>

<task type="auto">
  <name>Task 2: PPR Audit and Suspense Boundaries</name>
  <files>src/app/layout.tsx, src/app/messages/[chatId]/page.tsx</files>
  <action>
    1. Review `src/app/layout.tsx` and main page containers.
    2. Ensure that dynamic components (like the notification bell or user-specific sidebar items) are wrapped in `<Suspense>` with appropriate fallbacks (using the `Skeleton` component from Phase 3).
    3. Verify that the static shell of the dashboard streams instantly.
  </action>
  <verify>
    <automated>grep "Suspense" src/app/layout.tsx</automated>
  </verify>
  <done>Dynamic boundaries are correctly Suspended to enable efficient PPR.</done>
</task>

<task type="auto">
  <name>Task 3: Update GQL Queries for BlurHash</name>
  <files>dataconnect/main/operations.gql</files>
  <action>
    1. Update `ListDirectMessages`, `ListGroupMessages`, and `ListPublicMessages` queries in `operations.gql` to include the `blurHash` field.
    2. Update profile fetching queries if applicable.
    3. Run `npx firebase dataconnect:sdk:generate` to update generated code.
  </action>
  <verify>
    <automated>grep "blurHash" dataconnect/main/operations.gql</automated>
  </verify>
  <done>GQL queries now fetch BlurHash data for the UI.</done>
</task>

<task type="auto">
  <name>Task 4: Implement BlurHash Previews in MessageItem</name>
  <files>src/components/MessageItem.tsx</files>
  <action>
    1. Update `MessageItem` to accept a `blurHash` prop from the message data.
    2. Use `react-blurhash` to render a placeholder when an image is present but not yet loaded.
    3. Ensure a smooth transition from BlurHash to the actual image.
  </action>
  <verify>
    <automated>grep "Blurhash" src/components/MessageItem.tsx</automated>
  </verify>
  <done>Images in the chat now show a BlurHash placeholder while loading.</done>
</task>

</tasks>

<verification>
Monitor Next.js build logs to ensure PPR is active for the audited routes.
</verification>

<success_criteria>
- "use cache" is used in src/lib/data.ts.
- Suspense boundaries are placed around dynamic content in layout/pages.
- Chat images show BlurHash placeholders.
</success_criteria>
