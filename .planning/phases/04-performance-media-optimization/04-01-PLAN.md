---
phase: 04-performance-media-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/lib/media.ts, src/lib/storage.ts, dataconnect/schema/schema.gql, dataconnect/main/operations.gql, src/components/ChatInput.tsx]
autonomous: true
requirements: [T4.1]
user_setup: []

must_haves:
  truths:
    - "Images are compressed before upload on the client side"
    - "BlurHash strings are generated for all uploaded images"
    - "Data Connect schema and operations support storing blurHash"
  artifacts:
    - path: "src/lib/media.ts"
      provides: "Image compression and BlurHash utilities"
    - path: "dataconnect/schema/schema.gql"
      provides: "Updated schema for BlurHash storage"
    - path: "dataconnect/main/operations.gql"
      provides: "Mutations updated to accept blurHash"
  key_links:
    - from: "src/lib/media.ts"
      to: "browser-image-compression"
      via: "import"
    - from: "src/lib/media.ts"
      to: "blurhash"
      via: "import"
---

<objective>
Setup the foundation for media optimization by installing required libraries, creating utility functions for compression and BlurHash, updating the database schema, and integrating compression into the upload flow.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-performance-media-optimization/04-RESEARCH.md
@src/lib/storage.ts
@dataconnect/schema/schema.gql
@src/components/ChatInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create media utilities</name>
  <files>package.json, src/lib/media.ts</files>
  <action>
    1. Install `browser-image-compression`, `blurhash`, and `react-blurhash`.
    2. Create `src/lib/media.ts` exporting:
       - `compressImage(file: File): Promise<File>`: Uses browser-image-compression (max 1200px, 80% quality).
       - `generateBlurHash(file: File): Promise<string>`: Generates a BlurHash string (32x32) for the image.
  </action>
  <verify>
    <automated>npm list browser-image-compression blurhash react-blurhash</automated>
  </verify>
  <done>Dependencies installed and utility functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: Update Data Connect Schema and Operations</name>
  <files>dataconnect/schema/schema.gql, dataconnect/main/operations.gql</files>
  <action>
    1. Add `blurHash: String` field to `Student`, `DirectMessage`, `GroupMessage`, `PublicMessage`, and `Post` tables in `schema.gql`.
    2. Update `SendDirectMessage`, `SendGroupMessage`, and `SendPublicMessage` mutations in `operations.gql` to accept and store the `blurHash`.
    3. Run `npx firebase dataconnect:sdk:generate` to update generated code.
  </action>
  <verify>
    <automated>grep "blurHash: String" dataconnect/schema/schema.gql && grep "blurHash" dataconnect/main/operations.gql</automated>
  </verify>
  <done>Schema and operations updated to support BlurHash storage.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate compression into ChatInput upload flow</name>
  <files>src/components/ChatInput.tsx, src/lib/storage.ts</files>
  <action>
    1. Update `uploadChatMedia` in `src/lib/storage.ts` to optionally accept a `blurHash` string.
    2. Modify `src/components/ChatInput.tsx` to:
       - Compress images before calling `uploadChatMedia`.
       - Generate BlurHash during the compression step.
       - Pass both to the send handlers.
  </action>
  <verify>
    <automated>grep "compressImage" src/components/ChatInput.tsx</automated>
  </verify>
  <done>Client-side compression integrated into the chat upload flow.</done>
</task>

</tasks>

<verification>
Ensure `src/lib/media.ts` is type-safe and handles various image formats.
</verification>

<success_criteria>
- browser-image-compression, blurhash, and react-blurhash are in package.json.
- src/lib/media.ts exists with compression and hashing logic.
- Schema updated and generated.
- ChatInput compresses images before upload.
</success_criteria>
