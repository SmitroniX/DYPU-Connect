---
phase: 01-quality-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/lib/errors.ts, src/lib/security.ts, src/lib/security/constants.ts, src/lib/security/sanitization.ts, src/lib/security/fingerprint.ts, src/lib/security/profanity.ts, src/lib/deviceSessions.ts, src/lib/cookieShield.ts]
autonomous: true
requirements: [T1.2, T1.3, QUAL-03]

must_haves:
  truths:
    - "Security functions are modular and independent"
    - "Errors follow a consistent structure across the app"
    - "Circular dependencies between security modules are resolved"
  artifacts:
    - path: "src/lib/security/fingerprint.ts"
      provides: "Independent fingerprint logic"
    - path: "src/lib/errors.ts"
      provides: "Global AppError class and handlers"
    - path: "src/lib/security/profanity.ts"
      provides: "Modular profanity filtering"
  key_links:
    - from: "src/lib/deviceSessions.ts"
      to: "src/lib/security/fingerprint.ts"
      via: "import"
    - from: "src/lib/cookieShield.ts"
      to: "src/lib/security/fingerprint.ts"
      via: "import"
---

<objective>
Refactor core utility libraries into a modular structure and standardize error handling across the application.

Purpose: Reduce code coupling, improve maintainability, and ensure consistent error reporting.
Output: Decoupled security modules and global error handling pattern.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/spart/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-RESEARCH.md
@.planning/phases/01-quality-foundation/01-02-SUMMARY.md
@src/lib/security.ts
@src/lib/deviceSessions.ts
@src/lib/cookieShield.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modularize src/lib/security.ts</name>
  <files>src/lib/security.ts, src/lib/security/constants.ts, src/lib/security/sanitization.ts, src/lib/security/fingerprint.ts, src/lib/security/profanity.ts</files>
  <action>
    - Split `src/lib/security.ts` into a feature-based directory `src/lib/security/`.
    - Create `constants.ts` for `COOKIE_SECURITY`, `CSP_SOURCES`, `RATE_LIMITS`.
    - Create `sanitization.ts` for `sanitiseInput`, `hasDangerousContent`.
    - Create `fingerprint.ts` for `generateSessionFingerprint`.
    - Create `profanity.ts` for `filterProfanity`, `containsProfanity`.
    - Update `src/lib/security.ts` to re-export everything from sub-modules for backward compatibility, but mark for future removal or keep as a barrel file.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Security logic is modularized into focused sub-modules.</done>
</task>

<task type="auto">
  <name>Task 2: Decouple Library Dependencies</name>
  <files>src/lib/deviceSessions.ts, src/lib/cookieShield.ts, src/components/MessageItem.tsx</files>
  <action>
    - Update `src/lib/deviceSessions.ts` and `src/lib/cookieShield.ts` to import `generateSessionFingerprint` directly from `src/lib/security/fingerprint.ts`.
    - Update `src/components/MessageItem.tsx` (and others) to import `filterProfanity` from `src/lib/security/profanity.ts`.
    - Ensure `src/hooks` (`usePresence`, `useTypingStatus`) use modular imports.
    - Run a dependency check (e.g., using `madge` if installed, or just manual inspection) to ensure no circular dependencies.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Tight coupling between security and other libraries is reduced.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Global Error Handling</name>
  <files>src/lib/errors.ts, src/components/AuthProvider.tsx</files>
  <action>
    - Create `src/lib/errors.ts` defining a custom `AppError` class that includes a code, message, and optional original error.
    - Add a `handleError` utility that logs errors and shows appropriate toasts.
    - Refactor `AuthProvider.tsx`'s `mapFirestoreError` and `mapAuthError` to return `AppError` instances.
    - Use Zod's `.parse()` and `.safeParse()` error reporting within the global error handler.
    - Add a unit test `src/lib/errors.test.ts` to verify the error handling logic.
  </action>
  <verify>
    <automated>npm test src/lib/errors.test.ts</automated>
  </verify>
  <done>A robust, unified error handling pattern is implemented.</done>
</task>

</tasks>

<verification>
Ensure all modular imports work and the app builds.
Verify error handling via unit tests.
</verification>

<success_criteria>
- [ ] `security.ts` is split into smaller files.
- [ ] `AppError` class is used for structured error handling.
- [ ] Circular dependencies are non-existent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-03-SUMMARY.md`
</output>
