---
phase: 01-quality-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vitest.config.ts, src/components/AuthProvider.tsx, tsconfig.json]
autonomous: true
requirements: [T1.4, QUAL-02]

must_haves:
  truths:
    - "AuthProvider.tsx has no TypeScript errors"
    - "Vitest is configured and running"
    - "Project has a baseline test command"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Testing configuration"
    - path: "package.json"
      provides: "New scripts and dependencies"
    - path: "src/components/AuthProvider.tsx"
      provides: "Fixed auth logic with toast imports"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script"
---

<objective>
Establish project hygiene by resolving existing Lint/TSC errors and setting up the Vitest testing framework.

Purpose: Remove technical noise that blocks CI/CD and enable automated verification for future refactors.
Output: Zero-error baseline and functional testing environment.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/spart/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Vitest and Testing Dependencies</name>
  <files>package.json, vitest.config.ts</files>
  <action>
    - Install dev dependencies: `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`.
    - Create `vitest.config.ts` with React plugin and jsdom environment.
    - Add `"test": "vitest run"` and `"test:watch": "vitest"` to `package.json` scripts.
    - Create a simple smoke test in `src/lib/utils.test.ts` to verify the setup.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Vitest is configured and smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve AuthProvider.tsx TSC errors and Lint issues</name>
  <files>src/components/AuthProvider.tsx</files>
  <action>
    - Import `toast` from `react-hot-toast` in `src/components/AuthProvider.tsx`.
    - Fix any other TSC errors in the file (e.g., potential missing types or props).
    - Remove unused imports and variables flagged by ESLint in this file.
    - Ensure `firebaseReady` check correctly prevents crashes when env vars are missing.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>AuthProvider.tsx is error-free according to TSC.</done>
</task>

<task type="auto">
  <name>Task 3: Harden TypeScript and Global Lint Cleanup</name>
  <files>tsconfig.json, package.json</files>
  <action>
    - Enable `noUnusedLocals` and `noUnusedParameters` in `tsconfig.json`.
    - Run `npm run lint:fix` to automatically resolve simple lint issues (unused imports, etc.).
    - Manually resolve remaining lint issues identified in RESEARCH.md (target: 0 lint issues).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Project linting passes with strict unused variable checks enabled.</done>
</task>

</tasks>

<verification>
Run `npm run lint` and `npx tsc --noEmit` to ensure a clean state.
Run `npm test` to verify Vitest setup.
</verification>

<success_criteria>
- [ ] `npm run lint` returns 0 errors/warnings.
- [ ] `npx tsc --noEmit` returns 0 errors.
- [ ] `npm test` successfully executes tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-01-SUMMARY.md`
</output>
