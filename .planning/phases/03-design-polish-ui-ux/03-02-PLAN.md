---
phase: 03-design-polish-ui-ux
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/components/Sidebar.tsx, src/components/ChatHeader.tsx, src/components/ChatInput.tsx, src/app/error.tsx, src/app/loading.tsx]
autonomous: true
requirements: [T3.1, T3.2, T3.3]

must_haves:
  truths:
    - "Sidebar and Chat components use consistent design tokens for backgrounds and borders"
    - "Buttons and sidebar links respond to hover and tap with Framer Motion animations"
    - "The global error boundary uses an animated entrance and modern styling"
    - "Global loading state uses Skeleton loaders instead of just a spinner"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "Modern animated error boundary"
    - path: "src/app/loading.tsx"
      provides: "Skeleton-based global loading"
    - path: "src/components/Sidebar.tsx"
      provides: "Refined navigation with interactive feedback"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "framer-motion"
      via: "whileHover/whileTap"
    - from: "src/app/error.tsx"
      to: "framer-motion"
      via: "motion.div entrance"
---

<objective>
Refine core UI components for design consistency and implement interactive micro-animations. Modernize the global error and loading states to improve perceived quality.

Purpose: Achieve a "premium" feel through consistent styling and fluid feedback.
Output: Polished Sidebar and Chat components, plus animated error/loading boundaries.
</objective>

<execution_context>
@C:/Users/spart/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/spart/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-polish-ui-ux/03-RESEARCH.md
@src/components/Sidebar.tsx
@src/components/ChatHeader.tsx
@src/components/ChatInput.tsx
@src/app/error.tsx
@src/app/loading.tsx
@src/components/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Sidebar and Chat UI Consistency</name>
  <files>src/components/Sidebar.tsx, src/components/ChatHeader.tsx, src/components/ChatInput.tsx</files>
  <action>
    - Audit `Sidebar.tsx`, `ChatHeader.tsx`, and `ChatInput.tsx` to ensure they use CSS variables for colors (e.g., `var(--ui-bg-surface)`, `var(--ui-bg-hover)`).
    - Standardize border styles and radius to match a "Zinc" modern aesthetic.
    - Add `whileHover={{ scale: 0.98 }}` and `whileTap={{ scale: 0.95 }}` to navigation items and buttons using Framer Motion.
    - Ensure consistent spacing (gap/padding) across these core components.
  </action>
  <verify>
    <automated>grep -E "whileHover|whileTap" src/components/Sidebar.tsx</automated>
  </verify>
  <done>Core UI components are refined and interactive</done>
</task>

<task type="auto">
  <name>Task 2: Implement Modern Error and Loading Boundaries</name>
  <files>src/app/error.tsx, src/app/loading.tsx</files>
  <action>
    - Update `src/app/error.tsx` to use `motion.div` for a subtle entrance animation.
    - Style the error page with the centralized design tokens and a clear "Try Again" action.
    - Replace or augment the spinner in `src/app/loading.tsx` with a page-level Skeleton layout (e.g., Skeleton for sidebar and main content area).
    - Ensure `loading.tsx` provides an immediate visual response to navigation.
  </action>
  <verify>
    <automated>grep "motion\.div" src/app/error.tsx && grep "Skeleton" src/app/loading.tsx</automated>
  </verify>
  <done>Global error and loading states are modern and animated</done>
</task>

</tasks>

<verification>
Manual visual check of Sidebar animations.
Simulate error to verify `src/app/error.tsx` animation.
Trigger navigation to see `src/app/loading.tsx` skeleton layout.
</verification>

<success_criteria>
- Navigation items feel "tactile" with scale feedback.
- Loading doesn't cause a white flash; skeletons appear immediately.
- Design is visually consistent with the Neutral Dark theme.
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-polish-ui-ux/03-02-SUMMARY.md`
</output>
